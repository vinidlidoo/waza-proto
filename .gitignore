.env
.env.*
!.env.example

.DS_Store
*.swp

# Xcode (for when the iOS app lands)
xcuserdata/
*.xcuserstate
build/
.build/
.build-device/
DerivedData/

# iOS app secrets (LiveKit wsURL + publisher JWT). Regenerated by scripts/refresh-secrets.sh.
ios/WazaProto/WazaProto/Secrets.swift

# xcode-build-server state — contains a per-machine DerivedData hash.
ios/WazaProto/buildServer.json

# Locally-generated test assets (regenerated on demand by scripts/publish-test-pattern.sh)
assets/

# Local profiling captures (regenerated on demand by scripts/profile-video-quality.sh)
profiler/

# Vercel project (viewer/)
viewer/node_modules/
viewer/.vercel/

# Codex (codex-companion runtime cache)
.codex/

# Claude Code scheduler lock (transient per-process runtime artifact)
.claude/scheduled_tasks.lock

# Python coach agent (agent/) — plan 19
agent/.venv/
agent/**/__pycache__/
agent/coach.log
